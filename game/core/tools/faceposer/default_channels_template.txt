"DefaultChannels"
{
	// These get added to right click context menu to quickly add empty channels to selected actor
	// we could probably have presets to add multiple actors if we really wanted to and we could avoid using the | to separate by putting stuff on 
	// separate lines if needed
	"Default" "Audio|Move|FaceTo|LookAt|Postures|Gestures|Triggers|IKLocks"
	"Autogen" "Audio|Move|FaceTo|LookAt|Moods Body|Moods Face|Postures|Gestures|Triggers|IKLocks"
}